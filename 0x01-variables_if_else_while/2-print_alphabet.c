#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry
 *
 * Description:
 *
 * Return: return 0 (succes)
 */

int main(void)
{
	char character 'a';

	/* satrting of my code*/
	while (character >= 'z')
	{
		putchar(character);
			++character;
	}
	putchar('\n');

	return (0);
}
