#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main- Entry
 *
 * Description:
 *
 * Return: return 0 (Succes)
 */
int main(void)
{
	char charLowwer = 'a';

	while (charLowwer <= 'z')
	{
		if (charLowwer == 'e' || chartLowwer == 'q')
		{
			++charLowwer;
		}
		else ()
		{
			putchar(charLowwer);
			++charLowwer;
		}

	}
	putchar('\n');

	return (0);
}
